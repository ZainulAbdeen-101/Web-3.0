import { expect } from "chai";
import { ethers } from "hardhat";

// describe("Arrays", function () {
//   it("should return the newly changed ", async function () {
//     const Arrays = await ethers.getContractFactory("Arrays");
//     const arrays = await Arrays.deploy();
//     const myNumberarr=[0,1,2,3,4]
//     const setarrays = await arrays.addElement(myNumberarr);
//     await setarrays.wait();
//     const setnumbers= await arrays.setNumber(0,4)
//     await setnumbers.wait();

//     expect(await arrays.getLength()).to.equal(5)
//     expect(await arrays.getElement(0)).to.equal(0)
// // due to require conditional check
//     expect(await arrays.getNumberLength()).to.equal(5)
//     expect(await arrays.getNumber(0)).to.equal(4)
//   });
// });

// describe("Structs",function(){
// it("should return the same result",async function (){
//     const Structs=await ethers.getContractFactory("Structs")
//     const structs=(await Structs).deploy()
//     const set=await (await structs).setBook();
//    await set.wait()
  
//    expect(await (await structs).getBookId()).to.equal(1)
    
// })
// })



